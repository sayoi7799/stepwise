# 拾级 Stepwise 项目申报书

2026 年 9 月 24 日

## 基本信息

• 项目名称：拾级 Stepwise —— 中文分级阅读流水线

• 参赛者：【填你的名字】

• 联系方式：【填你的微信或邮箱】

• GitHub 仓库链接：https://github.com/sayoi7799/stepwise

• 项目方向：AI 应用（结合 AI 能力，解决真实需求的开源应用）

• 是否为移植项目：否。原创项目；依赖的第三方库在 `README.md` 与
  `data/README.md` 中逐一声明来源与许可证

## 项目简介

拾级 Stepwise 面向把中文当外语的学习者，解决的是「读中文原文读不下去」这件事。
现有的划词翻译和双语对照工具都在解决「让你看懂这一段」，却没有解决「让你积累
到能看懂下一段」，结果是读了半年仍然靠翻译活着。Stepwise 把「翻译」换成
「可理解输入」：先用词表驱动的分词器把文本切成词、对照学习者的 HSK 等级算出
已知词覆盖率，再挑出只比当前水平高一到三个生词的句子精读，最后让模型用中文
解释中文——而且模型的解释本身也要经过我们自己的分词器审计，超纲词太多就带着
更严的约束重新生成。项目用 MoonBit 实现，确定性的内核（分词、覆盖率、难度分档、
复习调度、EPUB 解析）与模型调用严格分离，80 个测试全部离线通过，不需要 API key
也能跑完整条流程。

## 核心功能范围

• 读取整本 EPUB：按规范解析 ZIP 容器、`container.xml`、OPF 清单与阅读顺序，按 spine 顺序逐章把 XHTML 提成纯文本，支持只看指定章节；

• 词表驱动的中文分词：基于 HSK 3.0 全量词表（10,978 词）做双向最大匹配，正向与反向各切一遍取最优，每个词带 HSK 等级和原文偏移；

• 阅读难度体检：算出已知词覆盖率，按泛读研究的经验区间分成「顺畅 / 需要支撑 / 偏难」三档，并输出逐章体检表，回答「这本书我从第几章开始读得动」；

• 精读句挑选：按句中生词数量打分，含一到三个生词的句子即为 i+1 甜区，优先推荐给学习者；

• 生词表与 Anki 导出：生词卡片背面是它出现的那句原话而不是中英对照，形成「句子挖矿」式卡片；

• 间隔重复调度：SM-2 算法的整数实现，时钟由调用方传入，同一份复习计划在任何机器上结果一致；

• OpenAI 兼容的模型客户端：默认指向 DeepSeek，支持 SSE 流式输出，报文组装与响应解析是带测试的纯函数；

• 输出可读性质量闸门：模型给出的解释会被送回分词器审计，覆盖率不达标时把具体的超纲词点名回灌给模型重试，而不是笼统地说「太难了」；

• 全程可离线验证：模型调用被压缩成一个 trait，测试用脚本化假客户端，CI 不需要网络也不需要 API key。

## 如何验证

```bash
# 80 个测试，全部离线
moon test

# 读一整本 EPUB，输出逐章体检表
moon run cmd/main -- analyze samples/sample.epub --level 2

# 真的调模型解释一个词（需要自备 DEEPSEEK_API_KEY）
moon run cmd/main -- explain 意思 "这句话很有意思。" --pinyin yìsi --word-level 3
```

前两条在干净机器上克隆仓库即可复现，不需要网络与密钥。
